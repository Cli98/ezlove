[pytest]
asyncio_mode = auto
# fixture 与测试统一 session 级事件循环：app.database 的全局 async engine
# 连接池绑定首个 loop，若 fixture（建库）与测试分属不同 loop 会触发
# "attached to a different loop"——统一 loop 是全局 engine 的必要配套
asyncio_default_fixture_loop_scope = session
testpaths = tests
